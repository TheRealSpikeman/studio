// src/lib/quiz-data/dummy-quizzes.ts
import type { QuizAdmin } from '@/types/quiz-admin';

// This data is no longer used, as we rely on the seed-quizzes file and localStorage
// as the single source of truth. The array is kept empty to prevent import errors in the project.
export const DUMMY_QUIZZES_DATA: QuizAdmin[] = [];
