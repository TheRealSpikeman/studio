// src/lib/quiz-data/seed-quizzes.ts

// This file is now obsolete and no longer used.
// Quiz data is now managed entirely through the UI and stored in localStorage.
// This prevents data inconsistencies and gives the admin full control.

export {}; // Export an empty object to satisfy module requirements.
