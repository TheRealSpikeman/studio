
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These are permissive rules for development.
    // For a production app, you should implement more granular security.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
