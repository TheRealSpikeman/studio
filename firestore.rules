
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for all users until a future date.
    // Replace with more specific rules before going to production.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 6, 28);
    }
  }
}
